[project]
name = "concurrent-starts-python-sdk"
version = "0.1.0"
description = "Start 100 sandboxes concurrently and report per-start and wall-clock timings"
requires-python = ">=3.11"
dependencies = [
    "together-sandbox>=4.0.1",
]
