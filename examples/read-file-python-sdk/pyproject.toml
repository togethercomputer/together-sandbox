[project]
name = "read-file-python-sdk"
version = "0.1.0"
description = "Build a snapshot from a Dockerfile, run a sandbox, and read a file from it"
requires-python = ">=3.11"
dependencies = [
    "together-sandbox>=4.0.1",
]
